discord.py==2.7.1
python-dotenv>=1.0

# The web app. aiohttp is already here via discord.py.
fastapi>=0.115
uvicorn[standard]>=0.30
jinja2>=3.1
itsdangerous>=2.2      # signs the session cookie for Starlette's SessionMiddleware
python-multipart>=0.0.9  # form posts on the login page
